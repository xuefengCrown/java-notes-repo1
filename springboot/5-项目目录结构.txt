
默认生成的 Spring Boot项目：
主程序已经生成好了，我们只需要我们自己的逻辑
resources文件夹中的目录结构
	static：保存所有的静态资源（js, css, images）
	templates: 保存所有的模板（Spring Boot默认jar包使用嵌入式的 Tomcat，默认不支持JSP页面）；可以使用模版引擎（freemaker，thymeleaf）
	application.properties: Spring Boot应用的配置文件
		
	
	
	
	